@model IEnumerable<OnlineShop.Models.Goods>

@foreach(var item in Model)
{
    <div class="col-md-4">
        @Html.DisplayFor(modelItem => item.Name)
        @Html.DisplayFor(modelItem => item.Price)
    </div>
}
